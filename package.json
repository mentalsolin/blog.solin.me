{
  "private": true,
  "scripts": {
    "start": "npx eleventy --serve --quiet",
    "build": "eleventy && gulp",
    "deploy": "npm run build && cd dist && rsync --archive --compress --delete . solin@vps:/var/www/beta.solinofficial.com"
  },
  "bugs": {
    "url": "https://github.com/SolinCode/solinofficial.com/issues"
  },
  "homepage": "https://solinofficial.com",
  "dependencies": {
    "@11ty/eleventy": "^1.0.2",
    "del": "^6.0.0",
    "gulp-clean-css": "^4.3.0",
    "gulp-copy": "^4.0.1",
    "gulp-htmlmin": "^5.0.1"
  },
  "devDependencies": {
    "gulp": "^4.0.2"
  }
}

<!DOCTYPE html>
<html class="page" lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta property="title" content="{{title}} — Solin's Blog"/>
    <meta property="description" content="{% if page.url != '/' %}{{preview}}{% else %}Оригинальный бложек mentalsolin'а.{% endif %}"/>

    <meta property="og:locale" content="ru_RU"/>
    <meta property="og:type" content="article"/>
    <meta property="og:title" content="{{title}}"/>
    <meta property="og:description" content="{% if page.url != '/' %}{{preview}}{% else %}Оригинальный бложек mentalsolin'а.{% endif %}"/>
    <meta property="og:image" content="{{"https://blog.solin.cc" + page.url + "images/preview.jpg"}}"/>
    <meta property="og:url" content="https://blog.solin.cc{% if page.url != '/' %}{{page.url}}{% endif %}"/>
    <meta property="og:site_name" content="Solin's Blog"/>

    {% if page.url != '/' %}
        <meta property="og:title" content="{{title}}">
        <meta property="og:type" content="article" />
        <meta property="og:description" content="{{preview}}">
        <meta property="og:image" content="{{"https://blog.solin.cc" + page.url + "images/preview.jpg"}}">
        <meta property="og:url" content="https://blog.solin.cc{{page.url}}">
        <meta name="twitter:card" content="summary_large_image">
    {% endif %}

    <meta name="theme-color" content="#fff6f5">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Montserrat:wght@200;300;400;500;600;700&family=Playfair+Display:wght@700&family=JetBrains+Mono:wght@300;400" rel="stylesheet">
    <link rel="stylesheet" href="/styles/styles.css">
    <link rel="stylesheet" href="/styles/print.css" media="print">
    <link rel="icon" type="image/x-icon" href="/images/icons/favicon.svg">

    <title>{{title}}{% if page.url != '/' %} — Solin's blog{% endif %}</title>
</head>
<body class="page__body">
    {% include 'header.njk' %}
    {{ content | safe }}
    {% include 'footer.njk' %}

    <script src="/scripts/logo.js"></script>
</body>
</html>

---
layout: page.njk
---

<main class="main">
    <div class="article__container container">
        <div class="container__inner">
            <div class="article__heading">
                <time class="article__heading--time" datetime="{{ date.toISOString() }}">{{ date | ruDate }}</time>
                <h1 class="article__heading--title">{{ title }}</h1>
                <p class="article__heading--preview">{{ preview }}</p>
                <p class="article__heading--tags">{{ tags | join(', ') }}</p>
                <img class="article__heading--image" src="images/preview.jpg" alt="{{title}}">
                <a class="article__heading--link" href="https://github.com/SolinCode/solinofficial.com/blob/main/src{{page.url}}index.md">Редактировать статью</a>
            </div>
            <div class="content">
                {{ content | safe }}
            </div>
        </div>
    </div>
</main>